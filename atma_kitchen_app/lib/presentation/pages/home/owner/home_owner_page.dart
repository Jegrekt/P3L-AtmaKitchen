import 'package:flutter/material.dart';

class HomeOwnerPage extends StatelessWidget {
  const HomeOwnerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Owner Page'),
      ),
    );
  }
}
